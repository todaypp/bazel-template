{
  "name": "cypress-example",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "clean": "rimraf dist node_modules",
    "install:clean": "rimraf yarn.lock && yarn clean && yarn install",
    "test": "bazel test //:all",
    "test:watch": "ibazel test //:all"
  },
  "dependencies": {
    "express": "4.17.2"
  },
  "devDependencies": {
    "@bazel/cypress": "5.0.0",
    "@bazel/ibazel": "0.15.10",
    "@bazel/typescript": "5.0.0",
    "@types/node": "14.14.31",
    "cypress": "9.4.1",
    "rimraf": "3.0.2",
    "rxjs": "7.5.2",
    "typescript": "4.5.5"
  }
}
